#include<stdio.h>
#define SIZE 10
int hash_table[SIZE];
void inti(){
	for(int i=0; i<SIZE; i++)
	{
	hash_table[i]=-1;
}
}
void insert (int key){
	int index=key%SIZE;
	while(hash_table[index]!=-1){
		index=(index+1)%SIZE;
	}
	hash_table[index]=key;
}
void display(){
	printf("\n hash_table:\n");
	for(int i=0; i<SIZE; i++)
	{
		printf("%d-->%d \n",i,hash_table[i]);
	}
}
int main(){
	int keys[]={23,43,13,27,39};
	int n=5;
	inti();
	for(int i=0; i<n; i++){
		insert(keys[i]);
	}
	display();
	
	return 0;
}
